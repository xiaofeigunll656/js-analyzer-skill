{
  "name": "js-analyzer-skill",
  "version": "0.1.0",
  "description": "Codex-only AI-led JavaScript project analysis skill producing one Chinese report with optional crypto helper.",
  "type": "module",
  "private": true,
  "scripts": {
    "leads": "node scripts/codex-js-leads.mjs",
    "validate": "node scripts/validate-outputs.mjs",
    "test": "node scripts/run-fixtures.mjs",
    "safety:check": "node scripts/check-public-safety.mjs --staged",
    "safety:check:worktree": "node scripts/check-public-safety.mjs"
  },
  "dependencies": {},
  "scarfSettings": {
    "enabled": false
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
